---
const {class: className } = Astro.props;
---
<h2 class={["font-bold", "text-2xl", "md:text-3xl", "my-4"].concat(className).join(' ')}>
    <slot/>
</h2>

<style>
  .cocktail-name {
    font-weight: 900;
  }
</style>
